const surveyMetadataPresets = [{}];

const SurveyMetadataPage = async ({
  params,
}: {
  params: { surveyUuid: string };
}) => {
  return <div className="flex h-full"></div>;
};

export default SurveyMetadataPage;
